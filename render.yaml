services:
  - type: web
    name: shaadi-planner-backend
    runtime: node
    region: virginia

    buildCommand: pnpm install --frozen-lockfile && pnpm prisma generate && pnpm prisma migrate deploy

    startCommand: pnpm server

    healthCheckPath: /api/v1/health

    envVars:
      - key: NODE_ENV
        value: production

      - key: DATABASE_URL
        fromDatabase:
          name: shaadi-planner-db
          property: connectionString

      - key: FRONTEND_URL
        value: https://YOUR-FRONTEND-URL.onrender.com

  - type: web
    name: shaadi-planner-frontend
    runtime: static
    region: virginia

    buildCommand: pnpm install --frozen-lockfile && pnpm build

    staticPublishPath: ./dist

    envVars:
      - key: VITE_API_URL
        value: https://YOUR-BACKEND-URL.onrender.com

    routes:
      - type: rewrite
        source: /*
        destination: /index.html

databases:
  - name: shaadi-planner-db
    databaseName: shaadi_planner
    user: shaadi_user
    region: virginia